# TruckTurn v5.3.1

本次发布将现有浩辰 CAD 5.3 功能移植到 AutoCAD，并按宿主版本分类交付。货车与跨运车的运动学和扫掠包络算法保持同一功能基线；新增 AutoCAD 2018、2019–2020、2021–2024、2025–2026、2027 五组编译目标。

AutoCAD 移植仅调整 CAD API 命名空间与选型窗的宿主模态调用，核心运动学和包络算法未改动。

## 功能

- `TRUCKDRIVE`：货车/铰接车连续驾驶、转弯、轮迹与包络。
- `TRUCKTURN90`：货车一键 90° 转弯。
- `CARRIERDRIVE` / `CAR`：集装箱跨运车路径模拟，包含多种转向模式、蟹行、车体与集装箱安全包络。
- 首尾节点或全部节点的车体显示切换；中间包络实时预览。

## 分发与验证

- `dist/AutoCAD/`：五组 AutoCAD 专用 DLL，基于相应年份的 Autodesk 官方托管 API 编译。
- `dist/GstarCAD/`：沿用原 5.3 浩辰 CAD 2025/2026 DLL，未重新修改其算法。
- 几何算法通过离线回归；AutoCAD 各年份仍需现场 `NETLOAD`、交互绘制及图面检查。不能把跨年份编译目标当作跨年份实机验证结果。

每次加载前先核对 CAD 年份和所选 DLL 所在文件夹。详细对应关系、操作步骤及构建方法见 `README.md`。
